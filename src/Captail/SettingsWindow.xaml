<Window x:Class="Captail.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource L.Brand}"
        Icon="pack://application:,,,/Captail;component/Assets/Captail.ico"
        Width="404" Height="650"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Foreground="{StaticResource TextPrimaryBrush}"
        FontFamily="{StaticResource FontUi}"
        Closing="Window_Closing"
        Deactivated="Window_Deactivated"
        PreviewMouseDown="Window_PreviewMouseDown"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Border x:Name="WindowFrame"
            Background="{StaticResource BgWindowGradient}"
            BorderBrush="{StaticResource WindowBorderBrush}"
            BorderThickness="1"
            CornerRadius="{StaticResource RadiusWindow}"
            RenderTransformOrigin="0.5,0.5">
        <Border.RenderTransform>
            <TranslateTransform x:Name="WindowShakeTransform"/>
        </Border.RenderTransform>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="54"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="34"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Margin="16,0"
                  Background="Transparent"
                  MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,10,0">
                    <Ellipse Stroke="{StaticResource AccentBrush}" StrokeThickness="2.4"
                             StrokeDashArray="15,5" StrokeStartLineCap="Round"/>
                    <Ellipse Width="5" Height="5" Fill="{StaticResource AccentBrush}"/>
                </Grid>
                <TextBlock Grid.Column="1" Text="{DynamicResource L.Brand}" FontSize="14.5" FontWeight="ExtraBold"
                           VerticalAlignment="Center"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <ToggleButton x:Name="LanguageButton"
                                  Style="{StaticResource LanguageTriggerButton}"
                                  Content="{DynamicResource L.LanguageCode}"
                                  FontSize="10.5"
                                  FontWeight="ExtraBold"
                                  IsChecked="{Binding IsOpen, ElementName=LanguagePopup, Mode=TwoWay}"
                                  ToolTip="{DynamicResource L.LanguageTip}"/>
                    <Button x:Name="SettingsButton" Style="{StaticResource IconButton}"
                            Click="Settings_Click" ToolTip="{DynamicResource L.Settings}">
                        <Path Data="{StaticResource IconGear}"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              StrokeThickness="1.8" Width="15" Height="15" Stretch="Uniform"/>
                    </Button>
                    <Button x:Name="CancelSettingsButton"
                            Visibility="Collapsed"
                            Style="{StaticResource SubtleButton}"
                            Content="{DynamicResource L.Settings.Cancel}"
                            Height="32"
                            Padding="11,0"
                            Margin="0,0,6,0"
                            FontSize="11.5"
                            Click="DiscardSettings_Click"/>
                    <Button x:Name="DoneButton"
                            Visibility="Collapsed"
                            Style="{StaticResource AccentButton}"
                            Content="{DynamicResource L.Done}"
                            Width="76"
                            Height="32"
                            Padding="12,0"
                            Margin="0,0,6,0"
                            FontSize="12.5"
                            Click="Apply_Click"/>
                    <Button Style="{StaticResource IconButton}" Click="Minimize_Click" ToolTip="{DynamicResource L.Minimize}">
                        <Path Data="M4 12h16" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              StrokeThickness="1.8" Width="13" Height="13" Stretch="Uniform"/>
                    </Button>
                    <Button Style="{StaticResource CloseIconButton}" Click="Close_Click" ToolTip="{DynamicResource L.Close}">
                        <Path Data="M6 6l12 12 M18 6L6 18"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              StrokeThickness="1.8" Width="13" Height="13" Stretch="Uniform"/>
                    </Button>
                </StackPanel>
            </Grid>

            <Popup x:Name="LanguagePopup"
                   Grid.Row="0"
                   PlacementTarget="{Binding ElementName=LanguageButton}"
                   Placement="Bottom"
                   HorizontalOffset="-220"
                   VerticalOffset="6"
                   AllowsTransparency="True"
                   StaysOpen="False"
                   PopupAnimation="None"
                   Opened="LanguagePopup_Opened"
                   Closed="LanguagePopup_Closed">
                <Border x:Name="LanguagePopupPanel"
                        Width="252"
                        Padding="9"
                        Background="#FF151A1D"
                        BorderBrush="{StaticResource WindowBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Opacity="0"
                        RenderTransformOrigin="0.5,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="30" ShadowDepth="8"
                                          Direction="270" Opacity="0.55"/>
                    </Border.Effect>
                    <Border.RenderTransform>
                        <TranslateTransform Y="-5"/>
                    </Border.RenderTransform>
                    <StackPanel>
                        <TextBlock Text="{DynamicResource L.Language.Title}"
                                   Margin="10,6,10,8"
                                   FontSize="11.5"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource TextSecondaryBrush}"/>
                        <ListBox x:Name="LanguageList"
                                 MaxHeight="430"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 ItemContainerStyle="{StaticResource LanguageOption}"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                 MouseLeftButtonUp="LanguageList_MouseLeftButtonUp"
                                 PreviewKeyDown="LanguageList_PreviewKeyDown"/>
                    </StackPanel>
                </Border>
            </Popup>

            <Grid x:Name="DashboardPanel" Grid.Row="1" Margin="16,4,10,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Margin="0,0,6,0">
                <Border Style="{StaticResource SettingsGroup}" Padding="14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid Width="46" Height="46" Margin="0,0,14,0">
                            <Ellipse Stroke="{StaticResource DividerBrush}" StrokeThickness="3"/>
                            <Ellipse x:Name="StatusRing" Stroke="{StaticResource AccentBrush}" StrokeThickness="3"
                                     StrokeDashArray="32,8" StrokeStartLineCap="Round"
                                     RenderTransformOrigin="0.5,0.5">
                                <Ellipse.RenderTransform>
                                    <RotateTransform x:Name="StatusRingRotation"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                            <Ellipse x:Name="StatusDot" Width="8" Height="8" Fill="{StaticResource AccentBrush}"/>
                        </Grid>
                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="StatusTitleText" Text="{DynamicResource L.Status.Enabled}"
                                       FontSize="14.5" FontWeight="Bold"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock x:Name="StatusDetailText" Text="{DynamicResource L.Status.Idle}"
                                       Style="{StaticResource SecondaryText}" Margin="0,3,0,0"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <ToggleButton x:Name="ReplayToggle" Grid.Column="2"
                                      Style="{StaticResource ToggleSwitch}"
                                      VerticalAlignment="Center"
                                      Click="ReplayToggle_Click"/>
                    </Grid>
                </Border>

                <Grid x:Name="DashboardAudioSourceHost" Margin="0,14,0,0"
                      Height="42" ClipToBounds="True">
                    <Grid x:Name="SimpleAudioSourcePanel">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="8"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ToggleButton x:Name="SystemSourceChip" Style="{StaticResource SourceChip}"
                                      Click="SourceChip_Click"
                                      ToolTip="{DynamicResource L.Audio.ToggleTip}">
                            <ToggleButton.ContextMenu>
                                <ContextMenu Style="{StaticResource TrayMenu}" Tag="system"
                                             Opened="AudioDeviceMenu_Opened"/>
                            </ToggleButton.ContextMenu>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Path Data="{StaticResource IconDisplay}"
                                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}"
                                      StrokeThickness="1.8"
                                      Width="15" Height="15" Stretch="Uniform"/>
                                <TextBlock x:Name="PrimaryAudioChipText" Grid.Column="1"
                                           Text="{DynamicResource L.Audio.System}" Margin="8,0,0,0"/>
                                <Ellipse x:Name="SystemSourceDot" Grid.Column="2" Width="6" Height="6"
                                         Margin="8,0,0,0" Fill="{StaticResource AccentBrush}"/>
                            </Grid>
                        </ToggleButton>
                        <ToggleButton x:Name="MicSourceChip" Grid.Column="2" Style="{StaticResource SourceChip}"
                                      Click="SourceChip_Click"
                                      ToolTip="{DynamicResource L.Audio.ToggleTip}">
                            <ToggleButton.ContextMenu>
                                <ContextMenu Style="{StaticResource TrayMenu}" Tag="microphone"
                                             Opened="AudioDeviceMenu_Opened"/>
                            </ToggleButton.ContextMenu>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Path Data="{StaticResource IconMic}"
                                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}"
                                      StrokeThickness="1.8"
                                      Width="15" Height="15" Stretch="Uniform"/>
                                <TextBlock Grid.Column="1" Text="{DynamicResource L.Audio.Microphone}" Margin="8,0,0,0"/>
                                <Ellipse x:Name="MicSourceDot" Grid.Column="2" Width="6" Height="6"
                                         Margin="8,0,0,0" Fill="{StaticResource AccentBrush}"/>
                            </Grid>
                        </ToggleButton>
                    </Grid>

                    <Border x:Name="PerAppAudioSourcePanel"
                            Visibility="Collapsed" Opacity="0"
                            Background="Transparent">
                        <Border.RenderTransform>
                            <TranslateTransform Y="4"/>
                        </Border.RenderTransform>
                        <ScrollViewer HorizontalScrollBarVisibility="Hidden"
                                      VerticalScrollBarVisibility="Disabled">
                            <StackPanel x:Name="PerAppAudioSourceItems"
                                        Orientation="Horizontal"/>
                        </ScrollViewer>
                    </Border>
                </Grid>

                <StackPanel Margin="4,13,4,0">
                    <Grid Margin="0,0,0,8">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Button x:Name="CodecSummaryButton" Style="{StaticResource DashboardLinkButton}"
                                    Click="CodecSummary_Click" ToolTip="{DynamicResource L.OpenCodec}">
                                <TextBlock x:Name="CodecSummaryText" Text="H.264"
                                           FontFamily="{StaticResource FontMono}" FontSize="11.5"/>
                            </Button>
                            <TextBlock Text=" · " FontFamily="{StaticResource FontMono}" FontSize="11.5"
                                       Foreground="{StaticResource TextMutedBrush}"/>
                            <Button x:Name="FpsSummaryButton" Style="{StaticResource DashboardLinkButton}"
                                    Click="FpsSummary_Click" ToolTip="{DynamicResource L.OpenFps}">
                                <TextBlock x:Name="FpsSummaryText" Text="60 FPS"
                                           FontFamily="{StaticResource FontMono}" FontSize="11.5"/>
                            </Button>
                        </StackPanel>
                        <TextBlock x:Name="DiskSummaryText" Text="—" HorizontalAlignment="Right"
                                   Style="{StaticResource SecondaryText}" FontSize="11.5"
                                   VerticalAlignment="Center"/>
                    </Grid>
                    <ProgressBar x:Name="DiskSummaryProgress" Style="{StaticResource DiskProgress}"
                                 Height="4" Minimum="0" Maximum="100"/>
                </StackPanel>

                <Grid Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="SaveReplayButton" Grid.Column="0"
                            Style="{StaticResource AccentButton}"
                            Height="44" Padding="12,0"
                            HorizontalContentAlignment="Stretch"
                            Click="SaveReplay_Click">
                        <Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <Path Data="{StaticResource IconDownload}"
                                      Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                      StrokeThickness="2"
                                      Width="15" Height="15" Stretch="Uniform" Margin="0,0,7,0"/>
                                <TextBlock x:Name="SaveButtonText" Text="Save 5 min"
                                           FontSize="13" FontWeight="ExtraBold" VerticalAlignment="Center"/>
                            </StackPanel>
                            <Border Background="#24000000" CornerRadius="6"
                                    Padding="6,3" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock x:Name="HotkeySummaryText" Text="Ctrl+Shift+F10"
                                           FontFamily="{StaticResource FontMono}" FontSize="9.5"
                                           VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Button>

                    <Button x:Name="RecordButton" Grid.Column="2"
                            Style="{StaticResource SubtleButton}"
                            Height="44" Padding="12,0"
                            HorizontalContentAlignment="Stretch"
                            Click="RecordButton_Click">
                        <Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Left">
                                <Ellipse x:Name="RecordDot" Width="9" Height="9"
                                         Fill="#FF5F63" Margin="0,0,7,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="RecordButtonText" Text="{DynamicResource L.Record.Start}"
                                           FontSize="13" FontWeight="ExtraBold" VerticalAlignment="Center"/>
                            </StackPanel>
                            <Border Background="#14FFFFFF" BorderBrush="{StaticResource BorderControlBrush}"
                                    BorderThickness="1" CornerRadius="6"
                                    Padding="5,2" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock x:Name="RecordHotkeySummaryText" Text="Ctrl+Shift+F11"
                                           FontFamily="{StaticResource FontMono}" FontSize="9.5"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Button>
                </Grid>

                <Grid Margin="2,10,2,0">
                    <Button x:Name="OutputFolderSummaryButton"
                            HorizontalAlignment="Left" Style="{StaticResource DashboardLinkButton}"
                            Click="OutputFolderSummary_Click" ToolTip="{DynamicResource L.OpenFolderSettings}">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Path Data="{StaticResource IconFolder}"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                  StrokeThickness="1.8" Width="13" Height="13" Stretch="Uniform"
                                  Margin="0,0,7,0"/>
                            <TextBlock x:Name="OutputFolderSummaryText" Style="{StaticResource MonoText}"
                                       FontSize="11.5" MaxWidth="340" TextTrimming="CharacterEllipsis"
                                       Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
                        </StackPanel>
                    </Button>
                </Grid>
                </StackPanel>

                <Grid Grid.Row="1" Margin="0,16,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Margin="2,0,8,8">
                        <TextBlock Text="{DynamicResource L.Library.Recent}"
                                   Style="{StaticResource SectionHeaderText}" Margin="0"
                                   FontSize="12.5" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <Button HorizontalAlignment="Right"
                                Style="{StaticResource DashboardLinkButton}"
                                Click="RefreshLibrary_Click"
                                ToolTip="{DynamicResource L.Library.Refresh}">
                            <TextBlock Text="↻" FontSize="16"/>
                        </Button>
                    </Grid>
                    <StackPanel x:Name="ReplayLibraryLoading" Grid.Row="1"
                                HorizontalAlignment="Center" VerticalAlignment="Center">
                        <Control Style="{StaticResource LoadingIndicator}"
                                 Width="24" Height="24" HorizontalAlignment="Center"/>
                        <TextBlock Text="{DynamicResource L.Library.Loading}"
                                   Style="{StaticResource SecondaryText}"
                                   Margin="0,10,6,0" TextAlignment="Center"/>
                    </StackPanel>
                    <TextBlock x:Name="ReplayLibraryEmptyText" Grid.Row="1"
                               Text="{DynamicResource L.Library.Empty}"
                               Style="{StaticResource SecondaryText}"
                               TextAlignment="Center" HorizontalAlignment="Center"
                               VerticalAlignment="Center" Margin="0,0,6,0"
                               Visibility="Collapsed"/>
                    <TextBlock x:Name="ReplayLibraryErrorText" Grid.Row="1"
                               Text="{DynamicResource L.Library.LoadFailed}"
                               Style="{StaticResource SecondaryText}"
                               TextAlignment="Center" HorizontalAlignment="Center"
                               VerticalAlignment="Center" Margin="0,0,6,0"
                               Visibility="Collapsed"/>
                <ListBox x:Name="RecentReplaysList" Grid.Row="1"
                         Background="Transparent" BorderThickness="0"
                         Padding="0,0,6,0" Visibility="Collapsed"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.CanContentScroll="True"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         ScrollViewer.ScrollChanged="RecentReplays_ScrollChanged">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <VirtualizingStackPanel/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter/>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border x:Name="ClipCard" Height="74" Margin="0,0,0,8"
                                    Background="{StaticResource BgSurfaceBrush}"
                                    BorderBrush="{StaticResource DividerBrush}"
                                    BorderThickness="1" CornerRadius="10" ClipToBounds="True">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="112"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Background="{StaticResource BgControlBrush}"
                                            CornerRadius="9,0,0,9">
                                        <Grid>
                                            <Border CornerRadius="9,0,0,9">
                                                <Border.Background>
                                                    <ImageBrush ImageSource="{Binding Thumbnail}"
                                                                Stretch="UniformToFill"/>
                                                </Border.Background>
                                            </Border>
                                            <Border HorizontalAlignment="Left" VerticalAlignment="Top"
                                                    Margin="5" Padding="4,1.5" CornerRadius="3"
                                                    Background="{Binding BadgeBackground}">
                                                <TextBlock Text="{Binding KindBadge}" FontFamily="{StaticResource FontMono}"
                                                           FontSize="9" FontWeight="Bold" Foreground="White"/>
                                            </Border>
                                            <Border HorizontalAlignment="Right" VerticalAlignment="Bottom"
                                                    Margin="5" Padding="5,2" CornerRadius="4"
                                                    Background="#BB0B0F11">
                                                <TextBlock Text="{Binding Duration}" FontFamily="{StaticResource FontMono}"
                                                           FontSize="10.5" Foreground="White"/>
                                            </Border>
                                        </Grid>
                                    </Border>
                                    <StackPanel x:Name="ClipText" Grid.Column="1" Margin="11,10"
                                                VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}" FontSize="13" FontWeight="SemiBold"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Details}" Style="{StaticResource SecondaryText}"
                                                   Margin="0,5,0,0" FontSize="11.5" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <Border x:Name="ClipActionTray" Grid.Column="1"
                                            HorizontalAlignment="Right" VerticalAlignment="Center"
                                            Margin="8" Padding="2" CornerRadius="8"
                                            Background="#F015191C" BorderBrush="{StaticResource BorderControlBrush}"
                                            BorderThickness="1" Opacity="0" IsHitTestVisible="False"
                                            RenderTransformOrigin="0.5,0.5">
                                        <Border.RenderTransform>
                                            <TranslateTransform x:Name="ClipActionTranslate" X="8"/>
                                        </Border.RenderTransform>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Style="{StaticResource ClipActionButton}"
                                                    Click="PlayReplay_Click" ToolTip="{DynamicResource L.Library.Preview}"
                                                    IsEnabled="{Binding CanTrim}">
                                                <Path Data="{StaticResource IconPlay}"
                                                      Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                      Stretch="Uniform"/>
                                            </Button>
                                            <Button Style="{StaticResource ClipActionButton}"
                                                    Click="RevealReplay_Click" ToolTip="{DynamicResource L.Library.Open}">
                                                <Path Data="{StaticResource IconFolder}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                      StrokeThickness="1.8" Stretch="Uniform"/>
                                            </Button>
                                            <Button Style="{StaticResource ClipActionButton}"
                                                    Click="TrimReplay_Click" ToolTip="{DynamicResource L.Library.Trim}"
                                                    IsEnabled="{Binding CanTrim}">
                                                <Path Data="{StaticResource IconScissors}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                      StrokeThickness="1.8" Stretch="Uniform"/>
                                            </Button>
                                            <Button Style="{StaticResource ClipActionButton}"
                                                    Click="RequestDeleteReplay_Click" ToolTip="{DynamicResource L.Library.Delete}">
                                                <Path Data="{StaticResource IconTrash}" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                      StrokeThickness="1.8" Stretch="Uniform"/>
                                            </Button>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <Trigger SourceName="ClipCard" Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ClipActionTray" Property="IsHitTestVisible" Value="True"/>
                                    <Setter TargetName="ClipCard" Property="Background" Value="{StaticResource BgHoverBrush}"/>
                                    <Setter TargetName="ClipCard" Property="BorderBrush" Value="#FF34423E"/>
                                </Trigger>
                                <EventTrigger SourceName="ClipCard" RoutedEvent="MouseEnter">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ClipActionTray"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="1" Duration="0:0:0.14"/>
                                            <DoubleAnimation Storyboard.TargetName="ClipActionTranslate"
                                                             Storyboard.TargetProperty="X"
                                                             To="0" Duration="0:0:0.14">
                                                <DoubleAnimation.EasingFunction>
                                                    <CubicEase EasingMode="EaseOut"/>
                                                </DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                            <DoubleAnimation Storyboard.TargetName="ClipText"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0.24" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                                <EventTrigger SourceName="ClipCard" RoutedEvent="MouseLeave">
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ClipActionTray"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="0" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="ClipActionTranslate"
                                                             Storyboard.TargetProperty="X"
                                                             To="8" Duration="0:0:0.1"/>
                                            <DoubleAnimation Storyboard.TargetName="ClipText"
                                                             Storyboard.TargetProperty="Opacity"
                                                             To="1" Duration="0:0:0.1"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                </Grid>
            </Grid>

            <Grid x:Name="SettingsPanel" Grid.Row="1" Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border x:Name="UnsavedChangesBar"
                        Grid.Row="0" Margin="16,4,10,8" Padding="11,9"
                        Visibility="Collapsed" Opacity="0"
                        Background="{StaticResource AccentChipBgBrush}"
                        BorderBrush="{StaticResource AccentChipBorderBrush}"
                        BorderThickness="1" CornerRadius="11">
                    <Border.RenderTransform>
                        <TranslateTransform x:Name="UnsavedChangesTranslate" Y="-6"/>
                    </Border.RenderTransform>
                    <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="{DynamicResource L.Settings.UnsavedTitle}"
                                       FontSize="11.5" FontWeight="Bold"
                                       Foreground="{StaticResource AccentBrush}"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{DynamicResource L.Settings.UnsavedHint}"
                                       FontSize="9.5"
                                       Foreground="{StaticResource TextSecondaryBrush}"
                                       TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </Border>
                <ScrollViewer x:Name="SettingsScrollViewer"
                              Grid.Row="1"
                              VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"
                              Padding="16,0,10,16">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource L.Section.Replay}" Style="{StaticResource SectionHeaderText}" Margin="2,8,2,8"/>
                        <Border Style="{StaticResource SettingsGroup}">
                            <StackPanel>
                                <Grid Margin="13,11">
                                    <TextBlock Text="{DynamicResource L.Replay.Instant}" Style="{StaticResource RowLabelText}"/>
                                    <ToggleButton x:Name="SettingsReplayToggle" HorizontalAlignment="Right"
                                                  Style="{StaticResource ToggleSwitch}"/>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Margin="0,0,12,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{DynamicResource L.Replay.WarnGameOff}"
                                                       Style="{StaticResource RowLabelText}"/>
                                            <Button Style="{StaticResource HelpIconButton}">
                                                <Button.ToolTip>
                                                    <ToolTip Style="{StaticResource HelpToolTip}">
                                                        <StackPanel>
                                                            <TextBlock Text="{DynamicResource L.Replay.WarnGameOff}"
                                                                       Style="{StaticResource HelpTipTitle}"/>
                                                            <TextBlock Text="{DynamicResource L.Help.WarnGameOff}"
                                                                       Style="{StaticResource HelpTipBody}"/>
                                                        </StackPanel>
                                                    </ToolTip>
                                                </Button.ToolTip>
                                            </Button>
                                        </StackPanel>
                                        <TextBlock Text="{DynamicResource L.Replay.WarnGameOffHint}"
                                                   Style="{StaticResource SecondaryText}"
                                                   FontSize="10" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <ToggleButton x:Name="WarnGameOffBox" Grid.Column="1"
                                                  Style="{StaticResource ToggleSwitch}"
                                                  VerticalAlignment="Center"/>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Margin="0,0,12,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{DynamicResource L.Replay.RecordingIndicator}"
                                                       Style="{StaticResource RowLabelText}"/>
                                            <Button Style="{StaticResource HelpIconButton}">
                                                <Button.ToolTip>
                                                    <ToolTip Style="{StaticResource HelpToolTip}">
                                                        <StackPanel>
                                                            <TextBlock Text="{DynamicResource L.Replay.RecordingIndicator}"
                                                                       Style="{StaticResource HelpTipTitle}"/>
                                                            <TextBlock Text="{DynamicResource L.Help.RecordingIndicator}"
                                                                       Style="{StaticResource HelpTipBody}"/>
                                                        </StackPanel>
                                                    </ToolTip>
                                                </Button.ToolTip>
                                            </Button>
                                        </StackPanel>
                                        <TextBlock Text="{DynamicResource L.Replay.RecordingIndicatorHint}"
                                                   Style="{StaticResource SecondaryText}"
                                                   FontSize="10" Margin="0,2,0,0"/>
                                    </StackPanel>
                                    <ToggleButton x:Name="RecordingIndicatorBox" Grid.Column="1"
                                                  Style="{StaticResource ToggleSwitch}"
                                                  VerticalAlignment="Center"/>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="190"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{DynamicResource L.Replay.IndicatorPosition}"
                                                   Style="{StaticResource RowLabelText}"/>
                                        <Button Style="{StaticResource HelpIconButton}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource HelpToolTip}">
                                                    <StackPanel>
                                                        <TextBlock Text="{DynamicResource L.Replay.IndicatorPosition}"
                                                                   Style="{StaticResource HelpTipTitle}"/>
                                                        <TextBlock Text="{DynamicResource L.Help.IndicatorPosition}"
                                                                   Style="{StaticResource HelpTipBody}"/>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                    <Border Grid.Column="1" Style="{StaticResource SegmentStrip}"
                                            IsEnabled="{Binding IsChecked, ElementName=RecordingIndicatorBox}">
                                        <UniformGrid x:Name="RecordingIndicatorPositionOptions" Rows="1">
                                            <RadioButton GroupName="RecordingIndicatorPosition" Tag="top-left"
                                                         Content="↖" ToolTip="{DynamicResource L.Position.TopLeft}"
                                                         Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="RecordingIndicatorPosition" Tag="top-right"
                                                         Content="↗" ToolTip="{DynamicResource L.Position.TopRight}"
                                                         Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="RecordingIndicatorPosition" Tag="bottom-left"
                                                         Content="↙" ToolTip="{DynamicResource L.Position.BottomLeft}"
                                                         Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="RecordingIndicatorPosition" Tag="bottom-right"
                                                         Content="↘" ToolTip="{DynamicResource L.Position.BottomRight}"
                                                         Style="{StaticResource SegmentButton}"/>
                                        </UniformGrid>
                                    </Border>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <StackPanel Margin="13,11">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="{DynamicResource L.Replay.BufferLength}" Style="{StaticResource RowLabelText}"/>
                                        <Button Style="{StaticResource HelpIconButton}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource HelpToolTip}">
                                                    <StackPanel>
                                                        <TextBlock Text="{DynamicResource L.Replay.BufferLength}" Style="{StaticResource HelpTipTitle}"/>
                                                        <TextBlock Text="{DynamicResource L.Help.BufferLength}" Style="{StaticResource HelpTipBody}"/>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                    <Border Style="{StaticResource SegmentStrip}">
                                        <UniformGrid x:Name="BufferOptions" Rows="1">
                                            <RadioButton GroupName="Buffer" Tag="15" Content="{DynamicResource L.Duration.15s}" Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="Buffer" Tag="30" Content="{DynamicResource L.Duration.30s}" Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="Buffer" Tag="60" Content="{DynamicResource L.Duration.1m}" Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="Buffer" Tag="120" Content="{DynamicResource L.Duration.2m}" Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="Buffer" Tag="300" Content="{DynamicResource L.Duration.5m}" Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="Buffer" Tag="600" Content="{DynamicResource L.Duration.10m}" Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="Buffer" Tag="900" Content="{DynamicResource L.Duration.15m}" Style="{StaticResource SegmentButton}"/>
                                        </UniformGrid>
                                    </Border>
                                </StackPanel>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="190"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center" Margin="0,0,12,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock x:Name="BufferLimitLabelText"
                                                       Text="{DynamicResource L.Replay.BufferLimit}"
                                                       Style="{StaticResource RowLabelText}"
                                                       TextWrapping="Wrap"/>
                                            <Button Grid.Column="1" Style="{StaticResource HelpIconButton}">
                                                <Button.ToolTip>
                                                    <ToolTip Style="{StaticResource HelpToolTip}">
                                                        <StackPanel>
                                                            <TextBlock Text="{DynamicResource L.Replay.BufferLimit}" Style="{StaticResource HelpTipTitle}"/>
                                                            <TextBlock Text="{DynamicResource L.Help.BufferLimit}" Style="{StaticResource HelpTipBody}"/>
                                                        </StackPanel>
                                                    </ToolTip>
                                                </Button.ToolTip>
                                            </Button>
                                        </Grid>
                                        <TextBlock x:Name="BufferLimitHintText"
                                                   Text="{DynamicResource L.Replay.BufferLimitHint}"
                                                   Style="{StaticResource SecondaryText}"
                                                   FontSize="10" Margin="0,2,0,0"
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                    <ComboBox x:Name="ReplaySizeLimitBox" Grid.Column="1"
                                              Style="{StaticResource DarkCombo}">
                                        <ComboBoxItem Tag="0" Content="{DynamicResource L.Replay.DurationOnly}"/>
                                        <ComboBoxItem Tag="250" Content="{DynamicResource L.Size.250MB}"/>
                                        <ComboBoxItem Tag="500" Content="{DynamicResource L.Size.500MB}"/>
                                        <ComboBoxItem Tag="1000" Content="{DynamicResource L.Size.1GB}"/>
                                        <ComboBoxItem Tag="2000" Content="{DynamicResource L.Size.2GB}"/>
                                        <ComboBoxItem Tag="5000" Content="{DynamicResource L.Size.5GB}"/>
                                        <ComboBoxItem Tag="10000" Content="{DynamicResource L.Size.10GB}"/>
                                    </ComboBox>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <TextBlock Text="{DynamicResource L.Replay.StartWindows}" Style="{StaticResource RowLabelText}"/>
                                    <ToggleButton x:Name="AutostartBox" HorizontalAlignment="Right"
                                                  Style="{StaticResource ToggleSwitch}"/>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="{DynamicResource L.Section.Video}" Style="{StaticResource SectionHeaderText}"/>
                        <Border x:Name="VideoSettingsGroup" Style="{StaticResource SettingsGroup}">
                            <StackPanel>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="190"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{DynamicResource L.Video.Source}" Style="{StaticResource RowLabelText}"/>
                                        <Button Style="{StaticResource HelpIconButton}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource HelpToolTip}">
                                                    <StackPanel>
                                                        <TextBlock Text="{DynamicResource L.Video.Source}" Style="{StaticResource HelpTipTitle}"/>
                                                        <TextBlock Text="{DynamicResource L.Help.VideoSource}" Style="{StaticResource HelpTipBody}"/>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                    <ComboBox x:Name="CaptureSourceBox" Grid.Column="1"
                                              Style="{StaticResource DarkCombo}"
                                              SelectionChanged="CaptureSourceBox_SelectionChanged">
                                        <ComboBoxItem Tag="desktop" Content="{DynamicResource L.Video.Desktop}"/>
                                        <ComboBoxItem Tag="game" Content="{DynamicResource L.Video.GameSource}"/>
                                    </ComboBox>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid x:Name="CodecSettingsRow" Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="190"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center" Margin="0,0,12,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{DynamicResource L.Video.Codec}" Style="{StaticResource RowLabelText}"/>
                                            <Button Style="{StaticResource HelpIconButton}">
                                                <Button.ToolTip>
                                                    <ToolTip Style="{StaticResource HelpToolTip}">
                                                        <StackPanel>
                                                            <TextBlock Text="{DynamicResource L.Video.Codec}" Style="{StaticResource HelpTipTitle}"/>
                                                            <TextBlock Text="{DynamicResource L.Help.VideoCodec.Av1}" Style="{StaticResource HelpTipBody}"/>
                                                            <TextBlock Text="{DynamicResource L.Help.VideoCodec.Hevc}" Style="{StaticResource HelpTipBody}" Margin="0,6,0,0"/>
                                                            <TextBlock Text="{DynamicResource L.Help.VideoCodec.H264}" Style="{StaticResource HelpTipBody}" Margin="0,6,0,0"/>
                                                        </StackPanel>
                                                    </ToolTip>
                                                </Button.ToolTip>
                                            </Button>
                                        </StackPanel>
                                        <TextBlock x:Name="HardwareEncoderText"
                                                   Style="{StaticResource SecondaryText}"
                                                   FontSize="10" Margin="0,2,0,0"
                                                   TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <ComboBox x:Name="CodecBox" Grid.Column="1"
                                              Style="{StaticResource DarkCombo}"
                                              SelectionChanged="CodecBox_SelectionChanged">
                                        <ComboBoxItem x:Name="H264CodecItem" Tag="h264"
                                                      Content="H.264 (AVC)"/>
                                        <ComboBoxItem x:Name="HevcCodecItem" Tag="hevc"
                                                      Content="H.265 (HEVC)"/>
                                        <ComboBoxItem x:Name="Av1CodecItem" Tag="av1"
                                                      Content="AV1"/>
                                    </ComboBox>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="190"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{DynamicResource L.Video.Bitrate}" Style="{StaticResource RowLabelText}"/>
                                        <Button Style="{StaticResource HelpIconButton}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource HelpToolTip}">
                                                    <StackPanel>
                                                        <TextBlock Text="{DynamicResource L.Video.Bitrate}" Style="{StaticResource HelpTipTitle}"/>
                                                        <TextBlock Text="{DynamicResource L.Help.Bitrate}" Style="{StaticResource HelpTipBody}"/>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                    <ComboBox x:Name="BitrateBox" Grid.Row="1" Grid.ColumnSpan="2"
                                              Style="{StaticResource DarkCombo}" Margin="0,8,0,0">
                                        <ComboBoxItem Tag="0" Content="{DynamicResource L.Video.AutoLoad}"/>
                                        <ComboBoxItem Tag="5" Content="5 Mbps"/>
                                        <ComboBoxItem Tag="10" Content="{DynamicResource L.Video.Mbps10}"/>
                                        <ComboBoxItem Tag="15" Content="15 Mbps"/>
                                        <ComboBoxItem Tag="20" Content="{DynamicResource L.Video.Mbps20}"/>
                                        <ComboBoxItem Tag="30" Content="30 Mbps"/>
                                        <ComboBoxItem Tag="40" Content="40 Mbps"/>
                                        <ComboBoxItem Tag="50" Content="{DynamicResource L.Video.Mbps50}"/>
                                        <ComboBoxItem Tag="65" Content="65 Mbps"/>
                                        <ComboBoxItem Tag="80" Content="{DynamicResource L.Video.Mbps80}"/>
                                        <ComboBoxItem Tag="100" Content="100 Mbps"/>
                                    </ComboBox>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid x:Name="NvencSettingsRow" Margin="13,11">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="190"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{DynamicResource L.Video.NvencMode}"
                                                   Style="{StaticResource RowLabelText}"/>
                                        <Button Style="{StaticResource HelpIconButton}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource HelpToolTip}">
                                                    <StackPanel>
                                                        <TextBlock Text="{DynamicResource L.Video.NvencMode}"
                                                                   Style="{StaticResource HelpTipTitle}"/>
                                                        <TextBlock Text="{DynamicResource L.Help.NvencLowOverhead}"
                                                                   Style="{StaticResource HelpTipBody}"/>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                    <ComboBox x:Name="NvencModeBox" Grid.Column="1"
                                              Style="{StaticResource DarkCombo}">
                                        <ComboBoxItem Tag="balanced" Content="{DynamicResource L.Video.NvencBalanced}"/>
                                        <ComboBoxItem Tag="low-overhead" Content="{DynamicResource L.Video.NvencLowOverhead}"/>
                                    </ComboBox>
                                    <CheckBox x:Name="LowOverheadAqBox" Grid.Row="1" Grid.ColumnSpan="2"
                                              Content="{DynamicResource L.Video.AdaptiveQuantization}"
                                              Style="{StaticResource CompactCheckBox}"
                                              Margin="0,8,0,0"/>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource L.Video.EstimatedRamBuffer}" Style="{StaticResource RowLabelText}"/>
                                        <TextBlock x:Name="RamEstimateText" Style="{StaticResource SecondaryText}"
                                                   FontSize="10" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="220"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource L.Video.Monitor}" Style="{StaticResource RowLabelText}"/>
                                    <Grid Grid.Column="1">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <ComboBox x:Name="MonitorBox" Style="{StaticResource DarkCombo}"/>
                                        <Button x:Name="IdentifyDisplaysButton"
                                                Grid.Column="1"
                                                Margin="6,0,0,0"
                                                Style="{StaticResource IdentifyDisplayButton}"
                                                Click="IdentifyDisplays_Click"
                                                ToolTip="{DynamicResource L.Video.IdentifyDisplays}"
                                                AutomationProperties.Name="{DynamicResource L.Video.IdentifyDisplays}">
                                            <Path Data="{StaticResource IconDisplay}"
                                                  Width="17" Height="17"
                                                  Stretch="Uniform"
                                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                                  StrokeThickness="1.7"
                                                  StrokeStartLineCap="Round"
                                                  StrokeEndLineCap="Round"
                                                  StrokeLineJoin="Round"/>
                                        </Button>
                                    </Grid>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="190"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid VerticalAlignment="Center" Margin="0,0,12,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock x:Name="ResolutionLabelText"
                                                   Text="{DynamicResource L.Video.Resolution}"
                                                   Style="{StaticResource RowLabelText}"
                                                   TextWrapping="Wrap"/>
                                        <Button Grid.Column="1" Style="{StaticResource HelpIconButton}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource HelpToolTip}">
                                                    <StackPanel>
                                                        <TextBlock Text="{DynamicResource L.Video.Resolution}" Style="{StaticResource HelpTipTitle}"/>
                                                        <TextBlock Text="{DynamicResource L.Help.Resolution}" Style="{StaticResource HelpTipBody}"/>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </Grid>
                                    <ComboBox x:Name="ResolutionBox" Grid.Column="1"
                                              Style="{StaticResource DarkCombo}">
                                        <ComboBoxItem Tag="source" Content="{DynamicResource L.Video.SourceResolution}"/>
                                        <ComboBoxItem Tag="720p" Content="1280 × 720"/>
                                        <ComboBoxItem Tag="1080p" Content="1920 × 1080"/>
                                        <ComboBoxItem Tag="1440p" Content="2560 × 1440"/>
                                        <ComboBoxItem Tag="2160p" Content="3840 × 2160"/>
                                    </ComboBox>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <StackPanel x:Name="FpsSettingsRow" Margin="13,11">
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                        <TextBlock Text="{DynamicResource L.Video.FrameRate}" Style="{StaticResource RowLabelText}"/>
                                        <Button Style="{StaticResource HelpIconButton}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource HelpToolTip}">
                                                    <StackPanel>
                                                        <TextBlock Text="{DynamicResource L.Video.FrameRate}" Style="{StaticResource HelpTipTitle}"/>
                                                        <TextBlock Text="{DynamicResource L.Help.FrameRate}" Style="{StaticResource HelpTipBody}"/>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                    <Border Style="{StaticResource SegmentStrip}">
                                        <UniformGrid x:Name="FpsOptions" Rows="1">
                                            <RadioButton GroupName="Fps" Tag="30" Content="30" Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="Fps" Tag="60" Content="60" Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="Fps" Tag="120" Content="120" Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="Fps" Tag="144" Content="144" Style="{StaticResource SegmentButton}"/>
                                            <RadioButton GroupName="Fps" Tag="240" Content="240" Style="{StaticResource SegmentButton}"/>
                                        </UniformGrid>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="{DynamicResource L.Section.Audio}" Style="{StaticResource SectionHeaderText}"/>
                        <Border Style="{StaticResource SettingsGroup}">
                            <StackPanel>
                                <StackPanel x:Name="SimpleSystemAudioPanel">
                                    <Grid Margin="13,11">
                                        <TextBlock x:Name="SystemAudioLabel" Text="{DynamicResource L.Audio.SystemAudio}"
                                                   Style="{StaticResource RowLabelText}"/>
                                        <ToggleButton x:Name="SystemAudioBox" HorizontalAlignment="Right"
                                                      Style="{StaticResource ToggleSwitch}" Click="AudioToggle_Click"/>
                                    </Grid>
                                    <ComboBox x:Name="AudioDeviceBox" Style="{StaticResource DarkCombo}" Margin="13,0,13,11"/>
                                    <Grid x:Name="SystemVolumeRow" Margin="13,0,13,11">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="70"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="42"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{DynamicResource L.Audio.Volume}" Style="{StaticResource SecondaryText}"
                                                   VerticalAlignment="Center"/>
                                        <Slider x:Name="SystemVolumeSlider" Grid.Column="1"
                                                Style="{StaticResource VolumeSlider}"/>
                                        <TextBlock Grid.Column="2"
                                                   Text="{Binding ElementName=SystemVolumeSlider, Path=Value, StringFormat={}{0:0}%}"
                                                   Style="{StaticResource MonoText}"
                                                   HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                    </Grid>
                                    <Border Style="{StaticResource RowDivider}"/>
                                </StackPanel>
                                <Grid Margin="13,11">
                                    <TextBlock Text="{DynamicResource L.Audio.Microphone}" Style="{StaticResource RowLabelText}"/>
                                    <ToggleButton x:Name="MicBox" HorizontalAlignment="Right"
                                                  Style="{StaticResource ToggleSwitch}" Click="AudioToggle_Click"/>
                                </Grid>
                                <ComboBox x:Name="MicDeviceBox" Style="{StaticResource DarkCombo}" Margin="13,0,13,11"/>
                                <Grid x:Name="MicVolumeRow" Margin="13,0,13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="42"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{DynamicResource L.Audio.Volume}" Style="{StaticResource SecondaryText}"
                                               VerticalAlignment="Center"/>
                                    <Slider x:Name="MicVolumeSlider" Grid.Column="1"
                                            Style="{StaticResource VolumeSlider}"/>
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding ElementName=MicVolumeSlider, Path=Value, StringFormat={}{0:0}%}"
                                               Style="{StaticResource MonoText}"
                                               HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                                <Grid x:Name="MicBoostRow" Margin="13,0,13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="95"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="48"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{DynamicResource L.Audio.Boost}" Style="{StaticResource SecondaryText}"
                                                   VerticalAlignment="Center"/>
                                        <Button Style="{StaticResource HelpIconButton}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource HelpToolTip}">
                                                    <StackPanel>
                                                        <TextBlock Text="{DynamicResource L.Audio.Boost}" Style="{StaticResource HelpTipTitle}"/>
                                                        <TextBlock Text="{DynamicResource L.Help.MicBoost}" Style="{StaticResource HelpTipBody}"/>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                    <Slider x:Name="MicBoostSlider" Grid.Column="1"
                                            Style="{StaticResource VolumeSlider}"
                                            Maximum="20" TickFrequency="1"
                                            IsSnapToTickEnabled="True"/>
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding ElementName=MicBoostSlider, Path=Value, StringFormat={}+{0:0} dB}"
                                               Style="{StaticResource MonoText}"
                                               HorizontalAlignment="Right" VerticalAlignment="Center"/>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="190"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center" Margin="0,0,12,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{DynamicResource L.Audio.Tracks}" Style="{StaticResource RowLabelText}"/>
                                            <Button Style="{StaticResource HelpIconButton}">
                                                <Button.ToolTip>
                                                    <ToolTip Style="{StaticResource HelpToolTip}">
                                                        <StackPanel>
                                                            <TextBlock Text="{DynamicResource L.Audio.Tracks}" Style="{StaticResource HelpTipTitle}"/>
                                                            <TextBlock Text="{DynamicResource L.Help.AudioTracks}" Style="{StaticResource HelpTipBody}"/>
                                                        </StackPanel>
                                                    </ToolTip>
                                                </Button.ToolTip>
                                            </Button>
                                        </StackPanel>
                                        <TextBlock x:Name="AudioTrackHintText" Text="{DynamicResource L.Audio.SystemAndMic}"
                                                   Style="{StaticResource SecondaryText}" Margin="0,3,0,0"/>
                                    </StackPanel>
                                    <ComboBox x:Name="AudioTrackModeBox" Grid.Column="1"
                                              Style="{StaticResource DarkCombo}"
                                              SelectionChanged="AudioTrackModeBox_SelectionChanged">
                                        <ComboBoxItem Tag="mixed" Content="{DynamicResource L.Audio.OneMixed}"/>
                                        <ComboBoxItem Tag="separate" Content="{DynamicResource L.Audio.Separate}"/>
                                        <ComboBoxItem x:Name="AdvancedAudioTrackItem" Tag="advanced"
                                                      Content="{DynamicResource L.Audio.PerAppRouting}"/>
                                    </ComboBox>
                                </Grid>
                                <Grid x:Name="AdvancedAudioRoutingRow" Margin="13,0,13,11"
                                      Visibility="Collapsed">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel VerticalAlignment="Center" Margin="0,0,12,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{DynamicResource L.Audio.ApplicationAudio}"
                                                       Style="{StaticResource RowLabelText}"/>
                                            <Button Style="{StaticResource HelpIconButton}">
                                                <Button.ToolTip>
                                                    <ToolTip Style="{StaticResource HelpToolTip}">
                                                        <StackPanel>
                                                            <TextBlock Text="{DynamicResource L.Audio.ApplicationAudio}" Style="{StaticResource HelpTipTitle}"/>
                                                            <TextBlock Text="{DynamicResource L.Help.ApplicationAudio}" Style="{StaticResource HelpTipBody}"/>
                                                        </StackPanel>
                                                    </ToolTip>
                                                </Button.ToolTip>
                                            </Button>
                                        </StackPanel>
                                        <TextBlock x:Name="AdvancedAudioRoutingSummaryText"
                                                   Style="{StaticResource SecondaryText}"
                                                   Margin="0,3,0,0"/>
                                    </StackPanel>
                                    <Button Grid.Column="1"
                                            AutomationProperties.AutomationId="ConfigureAudioRoutingButton"
                                            Content="{DynamicResource L.Audio.ConfigureRouting}"
                                            Style="{StaticResource SubtleButton}"
                                            Click="ConfigureAudioRouting_Click"/>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="190"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="{DynamicResource L.Audio.Codec}" Style="{StaticResource RowLabelText}"/>
                                        <Button Style="{StaticResource HelpIconButton}">
                                            <Button.ToolTip>
                                                <ToolTip Style="{StaticResource HelpToolTip}">
                                                    <StackPanel>
                                                        <TextBlock Text="{DynamicResource L.Audio.Codec}" Style="{StaticResource HelpTipTitle}"/>
                                                        <TextBlock Text="{DynamicResource L.Help.AudioCodec}" Style="{StaticResource HelpTipBody}"/>
                                                    </StackPanel>
                                                </ToolTip>
                                            </Button.ToolTip>
                                        </Button>
                                    </StackPanel>
                                    <ComboBox x:Name="AudioCodecBox" Grid.Column="1" Style="{StaticResource DarkCombo}"
                                              SelectionChanged="AudioCodecBox_SelectionChanged">
                                        <ComboBoxItem Tag="aac" Content="AAC"/>
                                        <ComboBoxItem Tag="opus" Content="Opus · MKV"/>
                                    </ComboBox>
                                </Grid>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="{DynamicResource L.Section.Storage}" Style="{StaticResource SectionHeaderText}"/>
                        <Border x:Name="StorageSettingsGroup" Style="{StaticResource SettingsGroup}">
                            <StackPanel>
                                <Grid x:Name="OutputFolderSettingsRow" Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Margin="0,0,10,0">
                                        <TextBlock Text="{DynamicResource L.Storage.ReplayFolder}" Style="{StaticResource RowLabelText}"/>
                                        <TextBlock x:Name="OutputDirText" Style="{StaticResource MonoText}" MaxWidth="250"/>
                                    </StackPanel>
                                    <Button x:Name="BrowseOutputButton" Grid.Column="1" Content="{DynamicResource L.Storage.Change}"
                                            Style="{StaticResource SubtleButton}"
                                            Click="BrowseOutput_Click"/>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Margin="0,0,12,0">
                                        <TextBlock Text="{DynamicResource L.Storage.OrganizeByGame}" Style="{StaticResource RowLabelText}"/>
                                        <TextBlock Text="{DynamicResource L.Storage.OrganizeByGameHint}"
                                                   Style="{StaticResource SecondaryText}" TextWrapping="Wrap" MaxWidth="285"/>
                                    </StackPanel>
                                    <ToggleButton x:Name="OrganizeByGameBox" Grid.Column="1"
                                                  Style="{StaticResource ToggleSwitch}" VerticalAlignment="Center"/>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <StackPanel Margin="13,11">
                                    <Grid Margin="0,0,0,8">
                                        <TextBlock x:Name="DiskUsedText" Style="{StaticResource SecondaryText}"/>
                                        <TextBlock x:Name="DiskFreeText" HorizontalAlignment="Right"
                                                   Style="{StaticResource SecondaryText}"/>
                                    </Grid>
                                    <ProgressBar x:Name="DiskProgress" Style="{StaticResource DiskProgress}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <TextBlock Text="{DynamicResource L.Section.Hotkeys}" Style="{StaticResource SectionHeaderText}"/>
                        <Border Style="{StaticResource SettingsGroup}">
                            <StackPanel>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource L.Hotkey.SaveReplay}" Style="{StaticResource RowLabelText}"/>
                                        <TextBlock Text="{DynamicResource L.Hotkey.SaveHint}" Style="{StaticResource SecondaryText}"/>
                                    </StackPanel>
                                    <Button x:Name="SaveHotkeyButton" Grid.Column="1"
                                            Style="{StaticResource SubtleButton}" MinWidth="128"
                                            Tag="save" Click="HotkeyCapture_Click"/>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource L.Hotkey.Toggle}" Style="{StaticResource RowLabelText}"/>
                                        <TextBlock Text="{DynamicResource L.Hotkey.ToggleHint}" Style="{StaticResource SecondaryText}"/>
                                    </StackPanel>
                                    <Button x:Name="ToggleHotkeyButton" Grid.Column="1"
                                            Style="{StaticResource SubtleButton}" MinWidth="128"
                                            Tag="toggle" Click="HotkeyCapture_Click"/>
                                </Grid>
                                <Border Style="{StaticResource RowDivider}"/>
                                <Grid Margin="13,11">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{DynamicResource L.Hotkey.Record}" Style="{StaticResource RowLabelText}"/>
                                        <TextBlock Text="{DynamicResource L.Hotkey.RecordHint}" Style="{StaticResource SecondaryText}"/>
                                    </StackPanel>
                                    <Button x:Name="RecordHotkeyButton" Grid.Column="1"
                                            Style="{StaticResource SubtleButton}" MinWidth="128"
                                            Tag="record" Click="HotkeyCapture_Click"/>
                                </Grid>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Grid>

            <Border Grid.Row="2" BorderBrush="{StaticResource DividerBrush}"
                    BorderThickness="0,1,0,0" Margin="16,0">
                <Grid>
                    <ToggleButton x:Name="AboutButton"
                                  HorizontalAlignment="Right"
                                  Style="{StaticResource FooterAboutButton}"
                                  IsChecked="{Binding IsOpen, ElementName=AboutPopup, Mode=TwoWay}">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="{StaticResource IconInfo}"
                                  Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ToggleButton}}"
                                  StrokeThickness="1.8" StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                  Width="13" Height="13" Stretch="Uniform"/>
                            <TextBlock Text="{DynamicResource L.Footer.About}"
                                       Margin="6,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </ToggleButton>
                    <Popup x:Name="AboutPopup"
                           PlacementTarget="{Binding ElementName=AboutButton}"
                           Placement="Custom"
                           CustomPopupPlacementCallback="AboutPopup_Placement"
                           AllowsTransparency="True"
                           StaysOpen="True"
                           PopupAnimation="None"
                           Opened="AboutPopup_Opened"
                           Closed="AboutPopup_Closed">
                        <Border x:Name="AboutPopupPanel"
                                Width="218" Padding="7"
                                Background="#FF151A1D"
                                BorderBrush="{StaticResource WindowBorderBrush}"
                                BorderThickness="1" CornerRadius="12"
                                Opacity="0" RenderTransformOrigin="0.5,1">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="26" ShadowDepth="7"
                                                  Direction="270" Opacity="0.52"/>
                            </Border.Effect>
                            <Border.RenderTransform>
                                <TranslateTransform Y="5"/>
                            </Border.RenderTransform>
                            <StackPanel>
                                <Button x:Name="GitHubButton"
                                        Style="{StaticResource AboutMenuButton}"
                                        Click="GitHub_Click"
                                        ToolTip="{DynamicResource L.Footer.GitHubTip}">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconGitHub}"
                                              Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                              Width="14" Height="14" Stretch="Uniform"/>
                                        <TextBlock Text="GitHub" Margin="9,0,0,0"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="ReportBugButton"
                                        Style="{StaticResource AboutMenuButton}"
                                        Click="ReportBug_Click"
                                        ToolTip="{DynamicResource L.Footer.ReportBugTip}">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconIssue}"
                                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                              StrokeThickness="1.8" StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                              Width="14" Height="14" Stretch="Uniform"/>
                                        <TextBlock Text="{DynamicResource L.Footer.ReportBug}"
                                                   Margin="9,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="FeatureRequestButton"
                                        Style="{StaticResource AboutMenuButton}"
                                        Click="FeatureRequest_Click"
                                        ToolTip="{DynamicResource L.Footer.FeatureTip}">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconFeature}"
                                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                              StrokeThickness="1.8" StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round" StrokeLineJoin="Round"
                                              Width="14" Height="14" Stretch="Uniform"/>
                                        <TextBlock Text="{DynamicResource L.Footer.Feature}"
                                                   Margin="9,0,0,0" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Popup>
                    <Button x:Name="UpdateVersionButton"
                            HorizontalAlignment="Left"
                            Style="{StaticResource UpdateVersionButton}"
                            ToolTipService.ShowOnDisabled="True"
                            Tag="current"
                            Click="UpdateVersion_Click">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse x:Name="UpdateStatusDot"
                                     Width="5" Height="5" Margin="0,0,6,0"
                                     Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                                     Visibility="Collapsed"/>
                            <TextBlock x:Name="UpdateStatusText"
                                       Text="v0.0.0"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <Border x:Name="NoticeBanner" Grid.RowSpan="3" Panel.ZIndex="20"
                    Visibility="Collapsed" Opacity="0"
                    VerticalAlignment="Bottom" Margin="16"
                    Background="#FF171C1F" BorderBrush="#FF5A3B35" BorderThickness="1"
                    CornerRadius="11" Padding="12">
                <Border.RenderTransform>
                    <TranslateTransform x:Name="NoticeTranslate" Y="12"/>
                </Border.RenderTransform>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Border Width="28" Height="28" CornerRadius="14" Background="#26E0A163"
                            BorderBrush="{StaticResource ErrorBrush}" BorderThickness="1">
                        <TextBlock Text="!" Foreground="{StaticResource ErrorBrush}"
                                   FontWeight="ExtraBold" FontSize="15"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="10,0">
                        <TextBlock x:Name="NoticeTitleText" Text="{DynamicResource L.Error}"
                                   FontSize="12.5" FontWeight="Bold"/>
                        <TextBlock x:Name="NoticeMessageText" Text=""
                                   Style="{StaticResource SecondaryText}"
                                   TextWrapping="Wrap" Margin="0,2,0,0"/>
                    </StackPanel>
                    <Button Grid.Column="2" Style="{StaticResource IconButton}"
                            Width="24" Height="24" Click="NoticeClose_Click">
                        <Path Data="M6 6l12 12 M18 6L6 18"
                              Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              StrokeThickness="1.8" Width="11" Height="11" Stretch="Uniform"/>
                    </Button>
                </Grid>
            </Border>

            <Border x:Name="DeleteConfirmOverlay" Grid.RowSpan="3" Panel.ZIndex="30"
                    Visibility="Collapsed" Background="#B80A0D0F" CornerRadius="15">
                <Border Width="320" Padding="18" HorizontalAlignment="Center" VerticalAlignment="Center"
                        Style="{StaticResource SettingsGroup}">
                    <StackPanel>
                        <TextBlock Text="{DynamicResource L.Library.DeleteTitle}"
                                   FontSize="15" FontWeight="Bold"/>
                        <TextBlock x:Name="DeleteConfirmFileText" Margin="0,7,0,0"
                                   Style="{StaticResource MonoText}" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{DynamicResource L.Library.DeleteMessage}" Margin="0,9,0,16"
                                   Style="{StaticResource SecondaryText}"/>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Content="{DynamicResource L.Library.Cancel}" Style="{StaticResource SubtleButton}"
                                    Margin="0,0,8,0" Click="CancelDeleteReplay_Click"/>
                            <Button Content="{DynamicResource L.Library.Delete}" Style="{StaticResource AccentButton}"
                                    Padding="14,8" Click="ConfirmDeleteReplay_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Border>
        </Grid>
    </Border>
</Window>
